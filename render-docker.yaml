services:
  - type: web
    name: car-rental-system
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production
